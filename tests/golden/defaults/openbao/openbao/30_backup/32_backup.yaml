apiVersion: v1
data:
  openbao-agent-config.hcl: |
    exit_after_auth = false
    auto_auth {
        method "kubernetes" {
            config = {
                role = "backup"
            }
        }
        sink "file" {
            config = {
                path = "/home/openbao/.vault-token"
                mode = 0644
            }
        }
    }
kind: ConfigMap
metadata:
  annotations: {}
  labels:
    name: foobar-backup
  name: foobar-backup
  namespace: openbao
---
apiVersion: k8up.io/v1
kind: PreBackupPod
metadata:
  name: foobar
  namespace: openbao
spec:
  backupCommand: bao operator raft snapshot save /dev/stdout
  fileExtension: .snapshot
  pod:
    spec:
      containers:
        - args:
            - agent
            - -config
            - /etc/openbao/openbao-agent-config.hcl
          env:
            - name: HOME
              value: /home/openbao
            - name: SKIP_SETCAP
              value: 'true'
            - name: VAULT_ADDR
              value: http://foobar-active:8200
          image: quay.io/openbao/openbao:2.5.0
          imagePullPolicy: IfNotPresent
          name: backup
          ports: []
          readinessProbe:
            exec:
              command:
                - test
                - -e
                - /home/openbao/.vault-token
            initialDelaySeconds: 3
            periodSeconds: 3
          stdin: false
          tty: false
          volumeMounts:
            - mountPath: /etc/openbao/
              name: config
            - mountPath: /home/openbao
              name: home
      imagePullSecrets: []
      initContainers: []
      serviceAccountName: foobar-backup
      terminationGracePeriodSeconds: 30
      volumes:
        - configMap:
            name: foobar-backup
          name: config
        - emptyDir: {}
          name: home
---
apiVersion: v1
kind: ServiceAccount
metadata:
  annotations: {}
  labels:
    name: foobar-backup
  name: foobar-backup
  namespace: openbao
